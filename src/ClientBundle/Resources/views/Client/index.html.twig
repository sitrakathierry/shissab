{% extends 'base.html.twig' %}

{% block scripts %}
    {{ parent() }}
    <script src="{{ asset('js/plugins/jqGrid/jquery.jqGrid.min.js') }}"></script>
    <script src="{{ asset('bundles/client/js/client.js') }}"></script>
    <script src="{{ asset('bundles/client/js/index.js') }}"></script>
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/plugins/jqGrid/ui.jqgrid.css') }}">
{% endblock %}

{% block contenu %}
    <div class="ibox float-e-margins">
        <div class="ibox-title">
            <h1>
                <i class="fa fa-user"></i>
                Clients
            </h5>
            <div class="ibox-tools">
                <a class="cl_edit_client cl_add btn btn-primary" data-type="0">
                    <i class="fa fa-plus"></i>
                    Ajouter
                </a>
                {#<a class="collapse-link">
                    <i class="fa fa-chevron-up"></i>
                </a>
                <a class="dropdown-toggle" data-toggle="dropdown" href="#">
                    <i class="fa fa-wrench"></i>
                </a>
                <ul class="dropdown-menu dropdown-user">
                    <li><a href="#">Config option 1</a>
                    </li>
                    <li><a href="#">Config option 2</a>
                    </li>
                </ul>
                <a class="close-link">
                    <i class="fa fa-times"></i>
                </a>#}
            </div>
        </div>
        <div class="ibox-content">
            <div class="row">
                <div class="col-lg-12">
                    <div class="checkbox checkbox-inline">
                        <input type="checkbox" class="cl_filtre_client" id="id_chk_personne" checked>
                        <label for="id_chk_personne">Personne</label>
                    </div>
                    <div class="checkbox checkbox-inline">
                        <input type="checkbox" class="cl_filtre_client" id="id_chk_societe" checked>
                        <label for="id_chk_societe">Soci&eacute;t&eacute;</label>
                    </div>
                </div>

                <div class="col-lg-12" id="id_container_table"></div>
            </div>
        </div>
    </div>
{% endblock %}