{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/plugins/jqGrid/ui.jqgrid.css') }}">
 {% endblock  %}

 {% block scripts %}
    {{ parent() }}
    <script src="{{ asset('js/plugins/jqGrid/jquery.jqGrid.min.js') }}"></script>
    <script src="{{ asset('bundles/client/js/list-type-social.js') }}"></script>
{% endblock %}

{% block contenu %}
    <div class="wrapper wrapper-content">
        <div class="row">
        	<div class="col-lg-12">
                <div class="ibox float-e-margins">
                    <div class="ibox-title">
                        <h5 style="font-size:20px">
                            <i class="fa fa-home"></i>
                            LISTE DES TYPES SOCIALE
                        </h5>
                        <div class="ibox-tools">
                            <a href="{{ path('typesocial_add') }}" class="btn btn-primary btn-xs" type="reset">
                                <i class="fa fa-plus"></i>
                                CREATION
                            </a>
                        </div>
                    </div>
                    <div class="ibox-content">
                        <!-- <div class="row">
                            <div class="col-lg-6">
                                    <a href="{{ path('typesocial_add') }}" class="btn btn-info" type="reset">
                                        <i class="fa fa-plus"></i>
                                        CREATION
                                    </a>
                            </div>
                        </div> -->
                    	<table id="list_type_social"></table>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

