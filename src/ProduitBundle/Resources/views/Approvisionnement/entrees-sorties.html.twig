<table class="table table-bordered">
    <thead>
        <tr>
            <th>Date</th>
            <th>Entrepôt</th>
            <th>Produit</th>
            <th>Qté ({{ produit.unite }})</th>
            <th>Prix unitaire</th>
            <th>Total</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {% for appro in approvisionnements %}
            <tr class="appro-{{appro.type}}">
                <td>{{ appro.date }}</td>
                <td>{{ appro.entrepot }}</td>
                <td> {{ appro.code_variation }} - {{ appro.nom }}</td>
                {# <td> {{ appro.code_variation }} - {{ appro.nom }} {{ appro.prix_vente }}</td> #}
                <td>{{ appro.qte }} </td>
                <td>{{ appro.prix }}</td>
                <td>{{ appro.total }}</td>
                <td>
                    {% if appro.type == 1 %}
                        ACHAT
                    {% endif %}

                    {% if appro.type == 2 %}
                        VENTE
                    {% endif %}

                </td>
            </tr>
        {% endfor %}
    </tbody>
</table>